# -----------------------------------------------------------------------------
# Environment Variables for Polarix Development
#
# Copy this file to '.env' and fill in the values if needed.
# Note: The default values in docker-compose.yml are used if this file is not present.
# -----------------------------------------------------------------------------

# --- PostgreSQL ---
# These are used by Keycloak and the Backend service.
# The user and initial 'keycloak' DB are created by the postgres:13 image.
POSTGRES_DB=keycloak
POSTGRES_USER=keycloak
POSTGRES_PASSWORD=password

# --- Keycloak ---
# Credentials for Keycloak to connect to Postgres.
KC_DB_USERNAME=keycloak
KC_DB_PASSWORD=password

# --- Backend ---
# Credentials for the backend to connect to Postgres.
DB_USER=keycloak
DB_PASSWORD=password

# -----------------------------------------------------------------------------
# Frontend Environment Variables
#
# These are NOT read by Docker Compose. Create a separate file at
# 'frontend/.env.local' for these.
# -----------------------------------------------------------------------------
# Example for frontend/.env.local:
# NEXT_PUBLIC_KEYCLOAK_URL=http://localhost:8081
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8080